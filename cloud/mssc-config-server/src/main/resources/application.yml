server:
  port: 9091
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/migmolrod/mssc-vault
          clone-on-start: true
          searchPaths: "{application}"
